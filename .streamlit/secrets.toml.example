# Example secrets file for Streamlit Cloud
# Copy to .streamlit/secrets.toml locally (git-ignored)
# Add your secrets to Streamlit Cloud dashboard for production

# Earth Engine Project ID
# ee_project_id = "your-ee-project-id"

# Google Cloud credentials (if needed for storage)
# gcs_bucket = "your-bucket-name"

# API Keys (if any external APIs are used)
# api_key = "your-api-key"
